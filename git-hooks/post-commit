#!/bin/bash
# Auto-update graphify knowledge graph after commits
# This hook updates the knowledge graph to keep it in sync with code changes

echo "Updating knowledge graph..."
graphify . --update --no-viz

if [ $? -eq 0 ]; then
    echo "✓ Knowledge graph updated successfully"
else
    echo "⚠ Knowledge graph update failed (non-blocking)"
fi
