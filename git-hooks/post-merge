#!/bin/bash
# Auto-update graphify knowledge graph after merges
# This hook updates the knowledge graph after pulling/merging changes

echo "Updating knowledge graph after merge..."
graphify . --update --no-viz

if [ $? -eq 0 ]; then
    echo "✓ Knowledge graph updated successfully"
else
    echo "⚠ Knowledge graph update failed (non-blocking)"
fi
